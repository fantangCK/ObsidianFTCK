# 一、神经网络的结构

[https://www.bilibili.com/video/BV1bx411M7Zx/](https://www.bilibili.com/video/BV1bx411M7Zx/)

1. **Neuron（神经元）**：简单地看是一个用来装数字的容器，装着0到1之间的数字。神经元中装着的数字代表对应像素的灰度值，也叫“激活值”，激活值越大，神经元越“亮”。神经网络运作的时候，上一层的激活值决定下一层的激活值。神经网络的处理信息的核心机制：一层的激活值是通过怎样的运算，算出下一层的激活值的。严谨地看其实是一个函数，它的输入是上一层所有神经元的输出
2. **为什么神经网络要分层？**以识别数字为例，将完整的数字拆成短边，再将短边结合为边，圆圈等笔画，最后一层将它们组合为数字进行判断。（将问题转化为抽象元素，一层层地抽丝剥茧）
3. **Weight（权重）**： 前一层的所有神经元和后一层的每个神经元之间的接线，都赋上权重值，然后求出他们的加权和。把关注区域的权重赋为正值，其他区域赋为0，然后对所有像素值取加权和,则只会累加关注区域的像素值。如果想识别此处是否真的存在一条边，就给周围一圈的像素赋予负的权重，此时权重值最大。
4. **Sigmoid函数**：把非常大的负值变成接近0，非常大的正值变成接近1，在取值0附近平稳增长。权重和任意大小，但是激活值都在0到1之间，此时就可以用Sigmoid函数来将其压缩。

![](https://cdn.nlark.com/yuque/0/2025/webp/43207844/1737007081608-2490b260-eb25-4c08-b0b1-bf5e8404bf5e.webp)

**5.Bias（偏置）**：当加权和大于某个数时，激发才有意义，则在加权和之后加上一个数，调整它的激发阈值。

**6.深度学习的Learning**：找到正确的权重和偏置。

![](https://cdn.nlark.com/yuque/0/2025/webp/43207844/1737007081633-ff153ffc-c731-420a-b2cf-af12ddf37080.webp)

**7.ReLU（线性整流函数）**：返回0和a的最大值，a就是输入。没过阈值，不激发，输出0.；过了阈值，则就是一个恒定的函数。在特别深的神经网络上效果尤其好。

![](https://cdn.nlark.com/yuque/0/2025/webp/43207844/1737007081606-e4bc52c1-9a06-41b8-a3ee-5e535630e171.webp)

# 二、梯度下降法

[https://www.bilibili.com/video/BV1Ux411j7ri](https://www.bilibili.com/video/BV1Ux411j7ri)

1. **Loss 损失**
2. **Cost 代价：**计算整体的代价
3. **Empirical Risk** 代价平均值/经验风险

![](https://cdn.nlark.com/yuque/0/2025/webp/43207844/1737008952538-0d1b8e8b-b65f-429d-a0f6-1aa46b27837a.webp)

  

![](https://cdn.nlark.com/yuque/0/2025/webp/43207844/1737008952618-83ee1e2e-0944-4764-b858-9599d0cc3034.webp)

  

![](https://cdn.nlark.com/yuque/0/2025/webp/43207844/1737008952715-e6d658d8-9945-43dc-a7d4-08414b13870b.webp)

通过代价让程序如果去更新权重和偏置才能减少损失。

如果定义了代价函数（损失函数），那么我们的目的就是找到这个函数的最小值；

4. 通过微积分，简单函数的最小值可以直接通过求导获得；
5. 简单的一元函数可以随意取值，判断斜率正负来决定往左还是右走（走的幅度尽量和斜率变化成比例，防止走过头），最终会得到一个斜率为0的点，但是一元函数通常不止一个拐点，通常只是一个局部最小值，而不是全局最小值；
6. 二元函数或者多元：函数的梯度决定了函数最陡的增长方向，它的负数代表的就是下降最快的方向

**Gradient descent（梯度下降法）：**


结构化的数据集更容易快速收敛到局部最小值

# 三、链式法则

# 四、反向传播算法

# 五、ReLU激活函数

# 六、神经网络多元输入输出

# 七、SoftMax 和 ArgMax

# 八、SoftMax导数

# 九、交叉熵 Cross Entropy

# 十、交叉熵+反向传播

# 十一、卷积神经网络CNN+图像识别

# 十二、张量 tensor

# 十三、PyTorch与优化神经网络

# 十四、RNN

# 十五、LSTM

# 十六、词嵌入、word2vec模型

# 十七、word embedding代码实现

# 十八、编码&解码神经网络，seq2seq模型

# 十九、注意力机制

# 二十、Transformer模型

# 二十一、Decoder-only Transformer模型