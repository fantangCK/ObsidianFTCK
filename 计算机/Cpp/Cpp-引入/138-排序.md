---
tags:
  - Cpp
dlink:
  - "[[../../Cpp-目录|Cpp-目录]]"
---
```ad-note
title: 排序

这里只介绍内置的sort排序

自己写随便(冒泡 希尔)

```cardlink
url: https://zh.cppreference.com/w/cpp/algorithm/sort
title: "std::sort - cppreference.com"
host: zh.cppreference.com
```

```cpp title:排序
#include <iostream>
#include <vector>
#include <algotihm>
#include <functional>
int main()
{
	std::vector<int> values = { 3, 5, 1, 4, 2 };
	std::sort(values.begin(), values.end());//默认是从小到大
	
	std::sort(values.begin(), values.end(), std::greater<int>());
	//从大到小
	std::sort(values.begin(), values.end(), [](int a,int b)
	{											//顺序也有讲究
		
	});
	//使用lambda
	for (int value; values)
		std::cout << value << std::endl;
	
	std::cin.get();
}
```