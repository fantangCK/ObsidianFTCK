---
tags:
  - Cpp
dlink:
  - "[[../../Cpp-目录|Cpp-目录]]"
---
```ad-note
title: 左值与右值

左值有实际存储空间

注意
- ~~等号右边就是右值~~
- 右值不只是字面量 也可以是函数返回结果

```

```cpp title:左值与右值
#include <iostream>

int Getvalue()
{
	return 10;
}

int& Getvalue() //左值引用
{
	static int value = 10;
	return value;
}

void SetValue(const int& value) //同时支持了左值和右值
{
}

void PrintName(std::string& name) 
{ 
	std::cout << "[Lvalue]" << name << std::endl; //左值引用
}

void PrintName(std::string&& name) 
{
	std::cout << "[Rvalue]" << name << std::endl; //双引用 右值引用
}	//不用临时变量转换(有点偷资源的味道)
				
void PrintName(const std::string& name) 
{ 
	std::cout << name << std::endl; 
}	//这里const不仅仅向编译器保证函数不修改实参 还允许实参为右值(做了临时变量的转换)
	//最终还是左值引用

int main()
{
	int i = 10;
	//左值 右值
	int a = i;
	int i = GetValue();
	
	const int& a = 10; //特殊规则 等价于
	/*
	int temp = 10; //创建了一个临时的左值
	const int& a  = temp;
	*/
	GetValue() = 5;
	
	SetValue(i); //i是左值
	SetValue(10); //10是临时右值 传入的时候被作为左值
	
	std::string firstName = "Yan";
	std::string lastName = "Chernikov";
	//这个例子中 左边都是左值 右边都是右值
	//注意firstName + lastName作为临时变量 也为右值
	std::string fullName = firstName + lastName;
	PrintName(fullName);
	//PrintName(firstName + lastName); //这里为右值无法传参
	//所以很多C++写 常量引用
	std::cin.get();
}
```

```ad-note
title: 持续集成
持续集成CI
帮助自动化整个过程,确保代码在所有平台和所有配置下都可以编译
并运行基本的自动化测试

流程
- 创建C++项目
- 每次提交到Git平台后 自动构建和测试应用
- 这里用Jenkins举例
	- 建立Jenkinsfile
	- 不啦不啦一堆 自己看配置(有米的话可以选择云服务器在线构建)

```cardlink
url: https://www.jenkins.io/
title: "Jenkins"
description: "Jenkins – an open source automation server which enables developers around the world to reliably build, test, and deploy their software"
host: www.jenkins.io
favicon: https://www.jenkins.io/favicon-32x32.png
image: https://www.jenkins.io/images/logo-title-opengraph.png
```


```ad-note
title: 静态分析
用静态分析器去检查代码
- 开源静态分析器推荐clang-tidy
- 商业类的如pvs studio

```


```ad-note
title: 参数计算顺序

后zi'zheng

```

```cpp title:参数计算顺序
#include <iostream>

void PrintSum(int a ,int b)
{
	std::cout << a << "+" << b << "=" << (a + b) << std::endl;
}

int main()
{
	int value = 0;
	PrintSum(value++, value++); 
	/*	这里是未定义行为
	*	debug下 1 + 0 = 1
	*	release下 0 + 0 = 0
	*/
	PrintSum(++value, ++value); 
	/*	这里是未定义行为
	*	debug下 4 + 4 = 8
	*	release下 4 + 4 = 8
	*/
	std::cin.get();
}
```

```ad-note
title: 移动语义
输入文字


```

```cpp title:移动语义
#include <iostream>

int main()
{

	std::cin.get();
}
```