---
tags:
  - Cpp
dlink:
  - "[[../../Cpp-目录|Cpp-目录]]"
---
```ad-note
title: 线程

并行运算


```cardlink
url: https://zh.cppreference.com/w/cpp/thread/thread
title: "std::thread - cppreference.com"
host: zh.cppreference.com
```

```cpp title:线程
#include <iostream>
#include <thread>

static bool s_Finished = false;

void DoWork()
{
	using namespace std::literals::chrono_literals; //chrono库提供了时间的表示
	
	std::cout << "Started thread id=" << std::this_thread::get_id() << std::endl;
	//打印线程的id
	
	while (!s_Finished)
	{
		std::cout << "Working..." << std::endl;
		std::this_thread::sleep_for(1s);	//每秒运行一次 (这里使用1s即chrono库)
	}
}

int main()
{
	std::thread worker(DoWork);	//一直都在DoWork
	
	std::cin.get(); //待输入后 将s_Finished赋值为1 停掉进程
	s_Finished = true;
	
	worker.join(); //阻塞进程 等待并发的进程全部结束后 再继续主进程
	td::cout << "Finished." << std::endl;
	
	std::cin.get();
}
```

```ad-note
title: 计时

记录代码运行时间

`chrono` c++11引入的库 
(在此11之前还要去使用操作系统库 高精确计时器还是要找操作系统库 系统库具体和平台相关 如Windows Linux等等)

```

```cpp title:计时
#include <iostream>
#include <chrono>
#include <thread>

int main()
{
	std::chrono::h
	std::cin.get();
}
```
