---
tags:
  - Cpp
dlink:
  - "[[../../Cpp-目录|Cpp-目录]]"
---
```ad-note
title:虚函数

## 1. 虚函数的特点

虚函数是父类中定义了**默认实现**的函数，但子类可以选择**重写（覆盖）**它，来实现自己的功能。其主要特点：

- **支持多态：** 允许父类指针或引用调用子类的实现，达到不同对象的不同表现。
- **可重写可不重写：** 子类可以根据需要选择是否重写父类的虚函数。
- **动态绑定：** 程序运行时决定调用哪个版本的函数，提升灵活性。

虚函数让代码更具扩展性和灵活性，是实现多态的核心机制。


## 2. virtual 和 override 的区别

- **virtual（虚函数声明）：** 用于父类，表示该函数可以在子类中被重写，支持多态。它告诉编译器：**“这个函数可能会被子类改写。”**

- **override（重写标志）：** 用于子类，明确表示这个函数是用来**重写父类中的虚函数**的，帮助编译器检查是否正确覆盖，避免因函数签名不一致导致的错误。

```

```cpp title:虚函数
#include <iostream>
#include <string>

class Entity
{
public://虚函数 virtual表(故有一定内存开销)
	virtual std::string getName(){ return "Entity"; }
};

class Player : public Entity
{
private:
	std::string m_Name;
public:
	Player(const std::string& name) //构造函数
		: m_Name(name) {} //成员变量初始化 相比于声明再赋值更高效

	std::string getName() override { return m_Name; } //c++11引入 增加可读性
						//复写的虚函数
	//std::string getName() { return m_Name; }
}

void PrintName(Entity* entity) 
{
	std::cout << entity->getNmae() << std::endl;
}

int main()
{
	Entity* e = new Entity(); //堆上声明的Entity
	PrintName(e);
	
	Player*p = new Player("Cherno")
	PrintName(p);//由于此处 函数的接口参数为父级Entity方法 输出的是Entity
	//使用虚函数后 Entity* entity 自动变为Player* entity
	//调用的也就是Player类中的函数
	
	std::cin.get();
}
```

```ad-note
title: 纯虚函数-接口

## 1. 什么是纯虚函数？

纯虚函数就是在父类中声明了一个函数，但不写具体内容，意思是告诉子类：**“这个功能你必须自己写好。”** 如果子类不实现这个函数，子类也无法正常使用。


## 2. 什么是抽象类？

包含纯虚函数的类叫**抽象类**，就像一个模板，**不能直接创建对象**，只能用来给子类做参考，确保子类实现必要的功能。


## 3. 纯虚函数的作用

纯虚函数的作用就是**规定规则**，确保子类实现特定功能，同时**支持多态**，让父类可以用相同方式调用不同子类的实现。


## 4. 接口类的特点

接口类通常**只包含纯虚函数**，就像合同，规定子类必须实现的功能。它**不能直接使用**，子类**必须实现**接口定义的内容，方便代码的扩展和管理。

```

```cpp title:纯虚函数
#include <iostream>
#include <string>


class Printable
{
public:
	virtual std::string getClassName() = 0; //有一个纯虚函数
}

class Entity : public Printable
{
public:
	virtual std::string getName() { return "Entity"; }
	std::string getClassName() override { return "Entity";}
};

class Player : public Entity
{
private:
	std::string m_Name;
public:
	Player(const std::string& name)
		: m_Name(name) {}

	std::string getName() override { return m_Name; }
	//std::string getClassName() override { return "Player";}
	//如果Player没有实现getClassName，则会调用父类Entity的getClassName方法
}

class A : public Printable
{
public:
	std::string getClassName() override { return "A";}
};

void Print(Printable* obj) //不在乎具体什么类
{
	std::cout << obj->getClassName() << std::endl;
}

int main()
{
	//Entity* e = new Printable(); //不成立 需要一个子类才能生成
	Entity* e = new Entity();
	
	Player*p = new Player("Cherno");
	
	Print(e);
	Print(p);
	Print(new A()); //实例,不要这么写,会有内存溢出
	//如果类中有实现Print的方法 即类中有getClassName()才能正常返回
	
	std::cin.get();
}
```