---
tags:
  - Cpp
dlink:
  - "[[../../Cpp-目录|Cpp-目录]]"
---
```ad-note
title: 类
Class 类比结构体
一群组合在一起的变量

```

```cpp title:类

class Player{
{
public:
	int x, y;
	int speed;
	
	void Move1(int xa, int ya) //在类里面就不必
	{}
};

void Move(Player& player, int xa, int ya)
{
	player.x += xa * player.speed;
	player.y += ya * player.speed;
}

int main(){
	Player player; //声明了一个对象
	player.x = 5;
	Move(player, 1, -1);
}
```