---
tags:
  - Cpp
dlink:
  - "[[../../Cpp-目录|Cpp-目录]]"
---
```ad-note
title: 类
Class 类比结构体
一群组合在一起的变量

```

```cpp title:类

class Player{
{
public:
	int x, y;
	int speed;
	
	void Move1(int xa, int ya) //在类里面就不必再调用Player
	{
	player.x += xa * player.speed;
	player.y += ya * player.speed;
	}
};

void Move(Player& player, int xa, int ya)
{
	player.x += xa * player.speed;
	player.y += ya * player.speed;
}

int main(){
	Player player; //声明了一个对象
	player.x = 5;
	Move(player, 1, -1);
	
	player.Move(1, -1);
}
```

```ad-todo
title: 类与结构体的区别

类在默认情况下是private（私有的）
结构体默认就是public的

```

```cpp title:习惯性处理方法

struct Vec2 //struct是不考虑继承的
{			//只用于存储数据
	float x, y;
	void Add(const Vec2& other)
	{
		x += other.x;
		y += other.y;
	} //这个函数也只是考虑数值 最后还是关于数值存储
}

```

```cpp title:类常见写法
class Log
{
public: //变量函数都可以分开写
	const int LogLevelError = 0;
	const int LogLevelWarning = 1;
	const int LogLevelInfo = 2;
private:
	int m_LogLevel = LogLevelInfo; //默认是设置为Info级别
	//m 指member 成员变量 -写法约定
public:
	void SetLevel(int level)
	 {
	 	m_LogLevel = level;
	 }
	 
	void Error(const char* message)
	{
		if(m_LogLevel >= LogLevelError)
			std::cout << "[ERROR]: " << message << std::endl;
	}	 
	
	void Info(const char* message)
	{
		if(m_LogLevel >= LogLevelInfo)
			std::cout << "[INFO]: " << message << std::endl;
	}	 
	 
	 void Warn(const char* message)
	 {
		 if(m_LogLevel >= LogLevelWarning)
		 	std::cout << "[WARNING]: " << message << std::endl;
	 }
};

int  main()
{
	Log log; //初始化
	//先明确需求再去补充clas内容
	log.SetLevel(log.LogLevelWarning); //输出日志等级
	log.Warn("hello!");
	log.Error("hello!");
	log.Info("hello!");
	std::cin.get();
}

```