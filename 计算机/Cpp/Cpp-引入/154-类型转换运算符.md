---
tags:
  - Cpp
dlink:
  - "[[../../Cpp-目录|Cpp-目录]]"
---
```ad-note
title: 类型转换运算符
输入文字


```

```cpp title:引入
#include <iostream>
#inlcude <memory>

struct Orange
{
	operator float() { return 5.f; } //orange结构体转换为float
}

void PrintFloat(float value)
{

}

int main()
{
	Orange orange;
	PrintFloat(orange);
	
	std::cout << orange << std::endl; 
	std::cin.get();
}
```

```cpp title:指针案例
#include <memory>

template<typename T>

class ScopedPtr
{
public:
	ScopedPtr() = default;
	ScopedPtr(T *ptr) : m_Ptr(ptr) {}
	~Scopedptr() { delete m_Ptr; } //自动析构

	bool IsValid() const { return m_Ptr != nullptr};	
	//新建函数判断也行 省事的方法如下
	T* Get() { return m_Ptr； }
	const T*Get () const { return m_Ptr; }
	
	operator bool() const { return m_Ptr != nullptr }; //这里类型转化操作符
	//同时也可以嵌套IsVaild使用
	//operator bool() const { return IsValid(); };

private:
	T*m_ptr nullptr;
};

struct Entity
{
	float X = 0.0f, Y = 0.0f;
};

void ProcessEntity(const ScopedPtr<Entity>& e)
{
	if(e) //如果这里用引用传入口参数时 直接调用e是不合法的
	{
		//...
	}
}

int main(){
	ScopedPtr<Entity> e = new Entity(); //作用域指针 会自动析构
	//std::unique_ptr<Entity> e(new Entity());
	//使用unique_ptr其类中已经包含了类型转换运算符 基本何ScopedPtr一致
	//注意这里要 显式构造
	ProcessEntity(e);
	
	Entity* entity = new Entity();
	
}
```